services:
  python:
    build:
      # ビルドコンテキストを ../python に更新
      context: ../python
      dockerfile: Dockerfile
    
    # 作業ディレクトリを /app に更新
    working_dir: /app
    
    # コンテナを開発用に起動し続ける
    command: sleep infinity

    volumes:
      # ホストの python ディレクトリ全体をコンテナの /app にマウント
      - ../python:/app

    ports:
      # TensorBoard 用のポートをホストに公開
      - "6006:6006"
      
    # (任意) コンテナ名を指定
    # container_name: 'tensorflow-pip-dev'